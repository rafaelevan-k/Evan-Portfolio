import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Rafael Evan Kristanto | Portfolio",
  description: "Portfolio of Rafael Evan Kristanto – Undergraduate Student in Information Systems with a passion for Frontend Web Development",
  keywords: ["portfolio", "developer", "software engineer", "web development", "Rafael Evan Kristanto"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.variable} antialiased`}>{children}</body>
    </html>
  );
}
